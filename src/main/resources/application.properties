spring.application.name=fashionithm

# Neo4j - MVP 에서 비활성화 (하드코딩 데이터 사용)
spring.neo4j.enabled=false
spring.data.neo4j.enabled=false

# Kafka - MVP 에서 비활성화
spring.kafka.enabled=false

# Server
server.port=8080

# Logging
logging.level.com.example.fashionithm=DEBUG
